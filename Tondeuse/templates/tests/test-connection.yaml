apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Tondeuse.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "Tondeuse.name" . }}
    helm.sh/chart: {{ include "Tondeuse.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "Tondeuse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
